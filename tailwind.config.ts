import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        background: "#0a0a0a",
        foreground: "#e6e6e6",
        'electric-blue': '#7df9ff'
      },
    },
  },
};

export default config;